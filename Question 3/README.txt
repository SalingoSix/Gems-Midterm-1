Camera can no longer be moved, but slowly moves to hover above the Tardis' location
Mouse can still control the direction the camera faces though.

The Tardis thread ensures it is always inside the maze, not inside a wall,
and always closing the gap between itself and the exit